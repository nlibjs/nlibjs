{
    "name": "@nlib/afs",
    "publishConfig": {
        "access": "public"
    },
    "version": "0.0.1",
    "main": "lib/index.js",
    "dependencies": {
        "@types/node": "^10.12.15"
    },
    "files": ["lib"],
    "scripts": {
        "cleanup": "rimraf lib",
        "build": "tsc",
        "test": "ava lib/*.test.js",
        "lint": "eslint --ext .ts src"
    },
    "eslintConfig": {
        "overrides": [
            {
                "files": ["src/core.ts"],
                "rules": {
                    "prefer-destructuring": "off"
                }
            }
        ]
    }
}

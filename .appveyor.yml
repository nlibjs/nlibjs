environment:
  matrix:
    - nodejs_version: '10'
install:
  - ps: Install-Product node $env:nodejs_version
  - npm install -g nyc
  - npm install
  - npm run bootstrap
test_script:
  - npm test
after_test:
  - npm install -g codecov
  - codecov
build: off

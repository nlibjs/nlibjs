{
    "private": true,
    "devDependencies": {
        "@types/eslint": "^4.16.5",
        "ava": "^1.0.1",
        "eslint": "^5.10.0",
        "lerna": "^3.6.0",
        "npm-run-all": "^4.1.5",
        "typescript": "^3.2.2"
    },
    "name": "nlibjs",
    "scripts": {
        "init": "lerna bootstrap",
        "build:cleanup": "rm -rf packages/*/lib",
        "build:tsc": "lerna exec tsc",
        "build": "npm-run-all --serial build:cleanup build:tsc"
    }
}
